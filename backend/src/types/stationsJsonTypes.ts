export type bpJson = {
  id: string;
  brand: string;
  name: string;
  lat: number;
  lng: number;
};

export type orlenJson = {
  id: number;
  brand: string;
  name: string;
  lat: string;
  lng: string;
  address: string;
  city: string;
  postcode: string;
  telephone: string;
};

export type shellJson = {
  id: string;
  brand: string;
  name: string;
  lat: number;
  lng: number;
  address: string;
  city: string;
  state: string;
  postcode: string;
  telephone: string;
};
